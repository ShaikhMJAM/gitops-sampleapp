apiVersion: apps/v1
kind: Deployment
metadata:
  name: sample-reactapp-59
spec:
  replicas: 1  # Number of pods to be created
  selector:
    matchLabels:
      app: sample-reactapp-59
  template:
    metadata:
      labels:
        app: sample-reactapp-59
    spec:
      containers:
      - name: sample-reactapp-59
        image: actdocker123/cicdpipeline:sample-reactapp-59
        ports:
        - containerPort: 3000
